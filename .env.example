# Copy this file to .env.local and fill in real values before running the app.

# GOOGLE_MAPS_PLATFORM_KEY (optional): enables the Google Maps vector map engine.
# If left blank, the app automatically falls back to the free
# OpenStreetMap/Leaflet map engine — no key required to run the project.
GOOGLE_MAPS_PLATFORM_KEY=""
